!!! Strict
%html{:lang => "ru", "xml:lang" => "en", :xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{:content => "text/html; charset=utf-8", "http-equiv" => "Content-Type"}/
    %title= isset($title) ? $title . ' | WayToChina.org' : 'Админ. панель | WayToChina.org'

    -#-------------- CSS -------------#-
    = html::style('public/css/admin.css')

    -#-------------- JS -------------#-
    = html::script('public/js/jquery.js')
    = html::script('public/js/jquery.json.js')
    = html::script('public/js/confirm.js')
    = html::script('public/js/jquery.tablednd_0_5.js')
    = html::script('public/js/admin.js')

  %body
    #menu
      %ul
        %li= html::anchor('/admin/business', 'Бизнес в Китае')
        %li= html::anchor('/admin/education', 'Образование в Китае')
        %li= html::anchor('/admin/translation', 'Услуги перевода')
        %li= html::anchor('/admin/questions', 'Вопросы')
        %li= html::anchor('/admin/requests', 'Заявки')
      %p.lang= i18n::$lang

    - if(isset($errors) && $errors)
      .error_box
        %ul
          - foreach($errors as $error)
            %li= $error
    - if(isset($messages) && $messages)
      .message_box
        %ul
          - foreach($messages as $message)
            %li= $message

    - if(isset($title))
      %h2= $title

    #content= $content
